plugins {
    id 'java'
}
bootJar { enabled = false; }
jar { enabled = true; }

dependencies {
    implementation project(':common')
    implementation project(':user')
}

test {
    useJUnitPlatform()
}