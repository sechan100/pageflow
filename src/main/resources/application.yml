spring:
  profiles:
    active: dev
    include:
      - secret
  thymeleaf:
    cache: false
    mode: HTML
    encoding: UTF-8
    prefix: classpath:templates
    suffix: .html

  output:
    ansi:
      enabled: always

logging:
  level:
    org:
#      root: DEBUG
#      pageflow:
#        domain:
#          book:
#            service: DEBUG
#      springframework:
#        security: TRACE
#        jdbc:
#          core: DEBUG
