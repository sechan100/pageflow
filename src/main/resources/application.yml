spring:
  profiles:
    active: dev
    include:
      - secret

  output:
    ansi:
      enabled: always

logging:
  level:
    org:
      pageflow:
        domain:
          book:
            service: DEBUG
#    org:
#      springframework:
#        security: DEBUG
#        jdbc:
#          core: DEBUG
